---
name: farm-issue
description: "File or revise a GitHub issue with the repository classification template and evidence-based acceptance. Use for authorized issue creation/update; do not automatically implement the issue."
---

# Maintain an issue contract

Read `.github/ISSUE_WORKFLOW.md`, choose the actual `.github/ISSUE_TEMPLATE/` file by deliverable, and search for duplicate/related issues. If these files are missing, report setup required rather than inventing a new competing template system.

Fill the body explicitly when creating through CLI/API. Separate observations, proposals, decisions, and unknowns; make acceptance verifiable. Assign one primary classification. Split only independent deliverables or approvals, not every procedural step.

Refresh the issue before updating. Preserve unrelated content, settled owner decisions, and approval history; update contradictory current text instead of layering an addendum over it. Serialize issue-body writes when conditional updates are unavailable. Read back the saved body and metadata.

Return identifiers and material changes. Never mark criteria complete without evidence, close a decision as implemented, or treat issue creation as budget/implementation authorization.
